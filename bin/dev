#!/usr/bin/env bash
set -e

echo "Starting Docker services..."
docker-compose up --build -d

echo "Installing frontend dev dependencies (if needed)..."
if [ ! -d node_modules ]; then
  npm install
fi

echo "Starting BrowserSync dev server (npm run dev)..."
npm run dev
